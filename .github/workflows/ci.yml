name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Test
    runs-on: macos-13
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Select Xcode
      run: sudo xcode-select -s /Applications/Xcode_15.0.app
    
    - name: Cache Swift packages
      uses: actions/cache@v3
      with:
        path: |
          ~/.swiftpm
          .build
        key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
        restore-keys: |
          ${{ runner.os }}-spm-
    
    - name: Install SwiftLint
      run: brew install swiftlint
    
    - name: Lint code
      run: swiftlint
    
    - name: Build
      run: swift build -v
    
    - name: Run tests
      run: swift test -v --enable-code-coverage
    
    - name: Generate coverage report
      run: |
        xcrun llvm-cov export \
          .build/debug/AURAPackageTests.xctest/Contents/MacOS/AURAPackageTests \
          -instr-profile .build/debug/codecov/default.profdata \
          -format="lcov" > coverage.lcov
    
    - name: Upload coverage
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.lcov
        fail_ci_if_error: true

  security-scan:
    name: Security Scan
    runs-on: macos-14
    steps:
    - uses: actions/checkout@v4

    - name: Run Security Scan
      run: |
        swift package resolve
        swift package update
        # Add any additional security scanning tools here
        # For example: OWASP dependency check, etc.

  performance-test:
    name: Performance Test
    runs-on: macos-14
    steps:
    - uses: actions/checkout@v4

    - name: Run Performance Tests
      run: |
        swift test --filter "Performance"
        # Add specific performance benchmarks here
        # For example: measure voice processing latency 